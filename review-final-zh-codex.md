# 何昊程中文简历最终审核

审核文件：`/home/h/code/resume/latex-templates/resume-zh.tex`

## 总体结论

当前中文简历信息完整度较高，核心经历覆盖航空仿真、工业工程、制造改善、AI/嵌入式工程与算法建模，适合投递航空运营、工业工程、仿真建模、数据/AI 工程助理等方向。简历已可用 XeLaTeX 编译为 1 页，整体版面紧凑。

主要风险不在经历本身，而在表达策略：个人简介偏模板化，部分技能措辞偏满；底部专利长段在 PDF 文本提取中出现顺序错位，存在 ATS 解析风险；AI 工程师助理经历缺少量化结果，和其他经历相比说服力略弱。

## 1. 个人简介审核

### 评价

- 能传达候选人的核心竞争力：工程技术背景、航空/制造/AI 交叉经历、编程与建模能力均有体现。
- 跨学科特点有体现：民航工程、工业工程、AI/算法、嵌入式/硬件、法学辅修共同构成差异化背景。
- 量化支撑不足：个人简介本身没有引用简历中已有的强数据，例如 4 类应急场景、5 种算法、线平衡率 58% 至 70%、人员 20 至 18、RUL 误差率约 8%、4 项专利、2 项软著。
- 语气基本自然，但有几处偏泛：`丰富的实习与科研经验`、`良好的团队协作能力和沟通技巧`、`多元化环境中高效工作` 属于常见套话。
- `精通 LSTM、BiLSTM、Transformer` 表述偏强。若主要来自课程设计或项目实践，建议改为 `熟悉` 或 `具备实践经验`，避免面试中被追问到理论细节时显得过度包装。

### 建议改写方向

建议把个人简介压缩为“定位 + 证据 + 技术栈”，减少泛化软技能。例如：

> 具备民航工程、工业工程与 AI 建模复合背景，研究与项目聚焦机场应急调度仿真、路径规划优化及设备剩余寿命预测。曾基于 AnyLogic/Java/MySQL 完成 4 类机场应急场景与 5 种路径规划算法对比，并在制造改善项目中验证线平衡率由 58% 提升至 70%。熟悉 Python、Java、SQL/MySQL、LSTM/BiLSTM/Transformer、Dijkstra/A*/GA 等工具与算法，具备从数据处理、建模仿真到工程验证的完整实践经验。

法学辅修可以保留，但建议根据岗位决定是否放入简介首段。若投递工程/AI 岗位，法学作为差异化补充即可，不必置于核心卖点前列。

## 2. 内容完整性

### 教育经历

- 教育经历完整：学校、地点、专业、学位、时间、GPA、论文题目均已列出。
- 硕士阶段建议将 `2025年9月 -- 2027年1月` 改为 `2025年9月 -- 2027年1月（预计）`，比单独在 GPA 后写 `在读` 更清晰。
- GPA 标注不完全统一：硕士写了 `3.95 / 4.3`，本科写 `3.67（前 20%）`，辅修写 `3.61`。建议本科和辅修都补充满绩点口径，例如 `3.67 / 5.0` 或学校实际口径。
- `法学 法学学士（辅修学位）` 有重复感，建议写为 `法学（辅修） 法学学士` 或 `法学辅修学士学位`。
- `优秀毕业论文 + 创新奖` 表达略口语化，建议明确奖项层级和正式名称，例如 `校级优秀毕业论文、毕业设计创新奖`。

### 实习经历

- 量化成果整体较好：科研助理有 `4 类场景、5 种算法、可靠指数 1.342`；美的有 `22 项改善方案、58% 至 70%、20 人至 18 人`；柳工有 `12 个工位、3 个瓶颈、节拍降低约 8%`。
- AI 工程师助理经历缺少量化结果，是当前最需要补强的经历。建议补充：测试覆盖了多少块 PCB/多少类模块、定位了多少个问题、固件实现了哪些控制功能、服务器维护规模、联调模块数量、代码审核或问题修复数量。
- `兼管服务器运维、网络设备配置、行政及技术面试` 信息跨度过大，容易显得职责发散。建议拆分或弱化行政内容，优先突出工程贡献。
- `理解整体系统架构` 不是成果型表达，建议改为更具体的行为：`梳理模块接口`、`定位联调问题`、`协助完成摄像头输入至推理模块链路验证` 等。

### 技能与成果

- 技能分类基本合理，但 `AI：LLM 工作流, Prompt Engineering, Loop Engineering` 这一行与前文经历关联较弱，且 `Loop Engineering` 不是常见标准技能名。若无项目支撑，建议删除或改为更具体的工具/框架。
- `算法与建模` 建议拆成 `机器学习/时序建模` 与 `路径规划/优化`，可读性更强。
- `工程与工具` 内容较杂，建议拆为 `仿真建模`、`嵌入式/硬件`、`开发工具`，便于 ATS 和招聘者快速匹配。

### 专利/软著

- 已列出 4 项专利和 2 项软著，内容较完整。
- 建议补充候选人在专利/软著中的角色或署名情况，例如 `第一发明人`、`共同发明人`、`主要完成人`。如果不是核心作者，也建议如实标注，避免面试风险。
- 专利标题较长，当前全部挤在一段中，阅读和 ATS 提取都不理想。建议改为短列表，或至少每项用分号后手动换行。

## 3. 语言质量

### 明显问题

- 暂未发现明显错别字。
- 部分表述偏模板化：`扎实的工程与技术背景`、`丰富的实习与科研经验`、`良好的团队协作能力和沟通技巧`。
- 部分表述偏口语或不够正式：`优秀毕业论文 + 创新奖` 中的 `+` 建议替换为顿号或正式奖项名称。
- `精通 LSTM、BiLSTM、Transformer` 语义过强，建议降为 `熟悉`、`掌握` 或 `具备实践经验`。
- `兼管` 可改为 `兼任` 或 `协助负责`，语气更职业。
- `行政及技术面试` 建议改为 `协助技术面试与候选人沟通`，否则“行政”在技术简历中价值较弱。

### 用词优化建议

- `擅长以跨学科的视角解决工程问题` 可改为 `能够结合仿真建模、算法优化与工程约束解决运营优化问题`，更具体。
- `能够独立完成数据分析、建模及优化任务` 可改为 `具备数据清洗、特征构建、模型训练、仿真验证与结果评估经验`。
- `理解整体系统架构` 可改为 `参与模块接口联调与问题定位`。

## 4. LaTeX 格式

### 编译与页数

- 使用 XeLaTeX 编译成功，生成 1 页 A4 PDF。
- 第二次编译后无明显 warning；日志中未发现 `Overfull` 或 `Underfull` 盒子警告。
- 字体嵌入正常，PDF 未加密。

### 版面问题

- 简历能完整显示在 1 页内，但底部 `技能与成果` 和 `专利/软著` 区域非常紧凑，视觉上接近页面下边界。
- `个人简介` 段落较长，且为整段文本，扫读效率一般。建议压缩到 2 至 3 行，释放底部空间。
- `专利` 段落过长，视觉上拥挤。建议改为小字号列表或精简为 `代表专利 4 项`，并只保留最相关的 2 至 3 项。
- 页面边距为 `margin=1cm, top=1cm, bottom=0.5cm`，底部 0.5cm 偏激进。若后续增加任何内容，建议将正文压缩后把 bottom 调回 0.7cm 至 0.8cm。
- 注释中使用大量 box drawing 字符，对编译没有影响，但若后续跨平台编辑，可考虑改成普通 ASCII 注释线，降低编码风险。

## 5. ATS 友好度

### 检查结果

- 源文件为 UTF-8，未检出零宽字符、BOM、不间断空格或双向控制字符。
- PDF 文本可以被 `pdftotext` 提取，姓名、联系方式、经历、技能等主体内容可读。
- 字体均已嵌入，且带 Unicode 映射，基础 ATS 可解析性较好。

### 主要 ATS 风险

- PDF 文本提取中，底部 `专利` 长段出现顺序错位：`2024）` 与后续专利号/标点在提取文本中被拆散。这可能影响 ATS 对专利信息的完整识别。
- 使用 `\hfill` 做左右排版一般可接受，但在部分 ATS 中可能造成时间、地点与职位顺序混乱。当前主要信息仍可读，风险可控。
- 简历没有使用表格和图片，这是优点；但过长单行和强制换行 `\\` 会增加文本顺序错乱概率，尤其在页面底部密集区域。

### ATS 优化建议

- 将 `专利` 和 `软著` 改成 itemize 列表，每条成果单独一行。
- 对职位、公司、地点、时间保持统一顺序，例如 `职位 | 公司 | 地点 | 时间`，避免 PDF 提取时左右栏错位。
- 技能关键词建议使用标准名称：`Python`、`Java`、`MySQL`、`AnyLogic`、`Plant Simulation`、`STM32`、`Ubuntu/Linux`、`Git`、`LSTM`、`Transformer`、`A*`、`Dijkstra` 等已经具备较好 ATS 价值。

## 优先修改清单

1. 重写个人简介，删除泛化软技能，加入 2 至 3 个量化证据。
2. 补强 AI 工程师助理经历的量化成果，至少增加 1 至 2 个可验证指标。
3. 将 `精通 LSTM、BiLSTM、Transformer` 调整为更稳妥的能力表述。
4. 重排 `专利/软著`，避免长段导致 ATS 文本顺序错位。
5. 统一 GPA 口径和奖项名称，增强教育经历的正式感。
6. 删除或解释 `Loop Engineering`，除非有明确项目证据支撑。

## 建议最终定位

这份简历最强的定位不是单纯“AI 工程师”，而是“航空/制造场景中的仿真建模、路径规划、数据分析与工程优化候选人”。若投递 AI 岗位，应突出时序建模、Python、嵌入式联调和工程落地；若投递航空运营/工业工程岗位，应突出 AnyLogic、路径规划、线平衡、精益改善和量化优化结果。
